# Multi-Core Simulation Dashboard (localhost GUI)

This project includes a Streamlit GUI that runs in your browser on `localhost`.

## Run the GUI

```powershell
python -m pip install -r requirements.txt
streamlit run app.py
```

Then open `http://localhost:8501`.

## One-command launcher (PowerShell)

```powershell
.\run_dashboard.ps1
```

